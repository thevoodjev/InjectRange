# Proposal: judge model

Status: closed, not planned. Routing probes through a model would make results
nondeterministic and the range unreproducible. Local guards keep the contract
that two runs are identical.
